# Synthesized minimal meta (writer's meta.yaml pending: the HPC profiling
# run is still on tp2; the tp1 CSV bundle is complete). Only keys the
# simulator consumes are provided. Variant 'default' = native mxfp4.
profiler_version: 1.0.0
vllm_version: 0.19.0
gpu: NVIDIA H100 80GB HBM3
hardware: H100
architecture: gpt_oss
model: openai/gpt-oss-120b
variant: default
tp_degrees: [1]
engine_effective:
  max_num_batched_tokens: 2048
  max_num_seqs: 256
skew_profile:
  enabled: false
skew_fit:
  enabled: false
